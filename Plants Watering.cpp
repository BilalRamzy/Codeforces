#include<bits/stdc++.h>

using namespace std;


int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;

    vector<long long> h(n), g(n);

    for (int i = 0; i < n; i++)
    {
        cin >> h[i];
    }
    
    for (int i = 0; i < n; i++)
    {
        cin >> g[i];
    }

    long long min_t = 0;
    long long max_t = LLONG_MAX;


    
}